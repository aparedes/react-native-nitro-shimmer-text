pre-commit:
  parallel: true
  jobs:
    - name: oxlint
      glob: '*.{js,jsx,ts,tsx}'
      run: bunx oxlint {staged_files}
    - name: oxfmt
      glob: '*.{js,jsx,ts,tsx}'
      run: bunx oxfmt --check {staged_files}
pre-push:
  jobs:
    - name: typecheck
      run: bun run typecheck
